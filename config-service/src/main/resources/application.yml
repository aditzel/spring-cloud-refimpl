server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aditzel/spring-cloud-refimpl-config.git
#      server:
#        native:
#          search-locations: file://${user.home}/.com.allanditzel/spring-cloud-refimpl/config
#          searchLocations: file://${user.home}/.com.allanditzel/spring-cloud-refimpl/config
